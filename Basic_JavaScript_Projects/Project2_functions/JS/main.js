function deadBug() {
    document.getElementById().innerHTML = "Now it's dead!"//Create deadBug function and will change text to Now its dead in the html//
}

function myFunction() {
    var sentence = "This part is ";
    sentence += " so very frustrating!";
    document.getElementById("concatenate").innerHTML = sentence;//function that joins two sentences//
}